import React from 'react';

import Logo from '../logo';

import "./style.scss"

const Header = () => {
  return (
    <header className="Header">
      <Logo />
    </header>
  );
}

export default Header;
